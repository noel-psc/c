# 以下是Visual Studio和C/C++项目核心的忽略规则
# 先忽略整个输出和中间文件目录（最大头）
[Bb]in/        # 编译输出目录（可执行文件、DLL等）
[Oo]bj/        # 中间编译对象目录（体积巨大）
[Oo]ut/        # 其他可能的输出目录

# 忽略Visual Studio的用户特定文件和缓存
.vs/           # VS 2015及以上版本的解决方案缓存、用户选项（非常重要！）
*.user         # 用户选项文件
*.userosscache
*.sln.docstates
*.suo          # 解决方案用户选项（旧版VS）

# 忽略编译和调试产物
*.exe          # 可执行文件
*.dll          # 动态链接库
*.lib          # 静态库
*.exp
*.ilk          # 增量链接文件
*.pdb          # 程序调试数据库（很大）
*.ipch         # VS智能感知预编译头缓存（巨大）
*.db           # 数据库文件
*.opendb
*.VC.db
